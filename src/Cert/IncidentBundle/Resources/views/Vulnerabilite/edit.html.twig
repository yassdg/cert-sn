{% extends '::layoutiAdmin.html.twig' %}

{% block body -%}
    <h1>Vulnerabilite edit</h1>
    <div class="col-md-5">
    	{{ form_start(edit_form, { 'attr' : {'id': 'form_admin_edit'}} ) }}
    
			{{ form_widget(edit_form.submit, {"attr": {"class":"display-none"}}) }}

	    {{ form_end(edit_form) }}

	    <div class="form-group">
			<ul  class="actions">
				<li>
			        <a href="{{ path('admin_vulnerabilite') }}" 
			        	class="btn 	 btn-warning">
			        	<i class="fa fa-chevron-left"></i>
			            Retour sur la liste
			        </a>    					
				</li>
				<li>
					<button type="submit" 
							form="form_admin_edit" 
							value="Submit" 
							class="btn btn-primary">
							Editer
					</button>
				</li>
				<li>
    				{{ form_start(delete_form) }}
    					{{ form_widget(delete_form.submit, {'attr': {'class': "btn btn-danger"} }) }}
    				{{ form_end(delete_form) }}
				</li>
			</ul>
		</div>
    </div>
    
{% endblock %}
