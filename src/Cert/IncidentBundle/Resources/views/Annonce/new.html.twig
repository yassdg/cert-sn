{% extends '::layoutiAdmin.html.twig' %}

{% block body -%}
		<h1 class="alert alert-info">Nouvelle Annonce</h1>    
	<div class="well">

    	{{ form_start(form, {'attr':{'class':'form-horizontal'}}) }}

	    	{# les erreurs generales du formulaire. #}
	    	<div class="alert alert-danger">
	    	zone pour message d'erreur<br/>
	    		{{ form_errors(form) }}
	    	</div>
	    	

	    	<div class="form-group">
	    		{# generation du label. #}
	    		{{ form_label(form.titre,"Titre de l'annonce",{'label_attr':{'class': 'col-sm-3 control-label'}}) }}

	    		{# affichage des erreurs pour ce champ title #}
	    		{{ form_errors(form.titre) }}

	    		<div class="col-sm-4">
	    			{# generation de l'input #}
	    			{{ form_widget(form.titre, {'attr': {'class': 'form-control'}}) }}
	    		</div>
	    	</div>

	    	{# meme chose pour un autre champ qui est image #}
	    	<div class="form-group">
	    		{# generation du label. #}
	    		{{ form_label(form.image,"Image de l'annonce",{'label_attr':{'class': 'col-sm-3 control-label'}}) }}

	    		{# affichage des erreurs pour ce champ title #}
	    		{{ form_errors(form.image) }}

	    		<div class="col-sm-4">
	    			{# generation de l'input #}
	    			{{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
	    		</div>
	    	</div>

	    	{# meme chose pour un autre champ qui est source #}
	    	<div class="form-group">
	    		{# generation du label. #}
	    		{{ form_label(form.source,"Source de l'annonce",{'label_attr':{'class': 'col-sm-3 control-label'}}) }}

	    		{# affichage des erreurs pour ce champ title #}
	    		{{ form_errors(form.image) }}

	    		<div class="col-sm-4">
	    			{# generation de l'input #}
	    			{{ form_widget(form.source, {'attr': {'class': 'form-control'}}) }}
	    		</div>
	    	</div>

	    	{# meme chose pour un autre champ qui est description#}
	    	<div class="form-group">
	    		{# generation du label. #}
	    		{{ form_label(form.description,"Description de l'annonce",{'label_attr':{'class': 'col-sm-3 control-label'}}) }}

	    		{# affichage des erreurs pour ce champ title #}
	    		{{ form_errors(form.description) }}

	    		<div class="col-sm-4">
	    			{# generation de l'input #}
	    			{{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
	    		</div>
	    	</div>
	    	{# meme chose pour un autre champ dateAnnonce #}
	    	<div class="form-group">
	    		{# generation du label. #}
	    		{{ form_label(form.dateAnnonce,"Date de pub Annonce",{'label_attr':{'class': 'col-sm-3 control-label'}}) }}

	    		{# affichage des erreurs pour ce champ title #}
	    		{{ form_errors(form.dateAnnonce) }}

	    		<div class="col-sm-4">
	    			{# generation de l'input #}
	    			{{ form_widget(form.dateAnnonce) }}
	    		</div>
	    	</div>
	    	
	    	{# generation de l'input submit pour notre cas #}
	    	{{ form_widget(form.submit,{'attr': {'class': 'btn btn-primary'}}) }}

	    	{# generation automatique des champs pas encore ecrits qui est le chapmp CSRF(generee automatiquement par symfony) et tous les champs cachees de type hidden #}

	    	{{ form_rest(form) }}

	    	{# fermeture de la balise <form> du formulaire html #}
	    {{ form_end(form) }}
    </div>
	
	<a href="{{ path('admin_annonce') }}" class="btn btn-sm btn-primary">
	<i class="fa fa-2x fa-edit"></i>Retourner &aacute; la liste
	    </a>

{% endblock %}
